# Secrets
.env

# Python
.venv/
__pycache__/
*.py[cod]

# Built artifacts and bulk corpora, rebuildable or too large for git.
# The synthetic needle set (legal_corpus/*.txt) IS committed — it is fixture
# data the eval depends on; only the fetched CUAD corpus stays out of git.
index_store/
legal_corpus/cuad/
data/
*.zip

# macOS
.DS_Store
